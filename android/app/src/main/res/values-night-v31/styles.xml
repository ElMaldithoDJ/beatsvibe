<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="LaunchTheme" parent="@android:style/Theme">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowSplashScreenBackground">@color/darkBackgroundColor</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/darkBackgroundColor</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/launch_background_night</item>
    </style>

</resources>